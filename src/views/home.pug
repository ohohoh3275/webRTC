doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
    body 
        header 
            h1 it works!
        main
            form(id="textForm")
                input(id="message" type="text", placeholder="write message")
                button(id="msgBtn" type="button" ) Send
        script(src="/public/js/app.js")